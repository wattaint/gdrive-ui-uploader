
import React, { Component } from 'react';

class Main extends Component {
  constructor(aProps) {
    super(aProps);
    this.state = {
    };
  }

  render() {
    return (
      <div className="App">
        TEST
      </div>
    );
  }
}

export default Main;
